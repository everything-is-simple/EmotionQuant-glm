# Spec Workflow Starter（Compatibility）

本文件用于兼容 `spec-system-prompt-loader` 的固定路径约定。

## Canonical Sources

请优先遵循以下权威文档：

1. `Governance/steering/6A-WORKFLOW.md`
2. `Governance/Capability/SPIRAL-CP-OVERVIEW.md`
3. `AGENTS.md`

## Spec Workflow Order

1. Requirements (`spec-requirements`)
2. Design (`spec-design`)
3. Implementation Plan (`spec-impl`)
4. Test Plan (`spec-test`)
5. Tasks (`spec-tasks`)
6. Review/Judge (`spec-judge`)

## Output Convention

- 统一输出到：`.claude/specs/{feature_name}/`
- 参考治理归档：`Governance/specs/`（若需对外同步，以治理要求为准）
